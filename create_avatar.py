from PIL import Image, ImageDraw, ImageFont
import os

def create_bot_avatar():
    """Создаем простой аватар для бота"""
    try:
        # Создаем изображение 640x640
        img = Image.new('RGB', (640, 640), color='#667eea')
        draw = ImageDraw.Draw(img)
        
        # Рисуем круг
        draw.ellipse([120, 120, 520, 520], fill='#764ba2', outline='white', width=10)
        
        # Рисуем иконку камеры
        # Корпус камеры
        draw.rectangle([240, 260, 400, 380], fill='white', outline='white', width=5)
        # Объектив
        draw.ellipse([280, 300, 360, 380], fill='#667eea', outline='white', width=5)
        # Вспышка
        draw.rectangle([340, 220, 380, 260], fill='yellow', outline='white', width=3)
        
        # Добавляем текст
        try:
            font = ImageFont.truetype("arial.ttf", 40)
        except:
            font = ImageFont.load_default()
        
        draw.text((320, 450), "PUBLISH", fill='white', font=font, anchor='mm')
        
        # Сохраняем
        img.save('bot_avatar.png')
        print("✅ Аватар создан: bot_avatar.png")
        
        # Показываем информацию об аватаре
        print(f"📏 Размер: {img.size[0]}x{img.size[1]} пикселей")
        print("🎨 Цвета: градиент фиолетовый-синий")
        print("📸 Иконка: камера для публикации контента")
        
        return True
        
    except Exception as e:
        print(f"❌ Ошибка создания аватара: {e}")
        
        # Создаем простой аватар как запасной вариант
        try:
            img = Image.new('RGB', (640, 640), color='#4285f4')
            draw = ImageDraw.Draw(img)
            draw.ellipse([220, 220, 420, 420], fill='#34a853')
            
            # Сохраняем
            img.save('bot_avatar.png')
            print("✅ Простой аватар создан как запасной вариант")
            return True
            
        except:
            return False

if __name__ == "__main__":
    print("🎨 СОЗДАНИЕ АВАТАРА ДЛЯ БОТА")
    print("=" * 50)
    
    if create_bot_avatar():
        print("=" * 50)
        print("✅ Аватар успешно создан!")
        print("📍 Файл: bot_avatar.png")
        print("📍 Размер: 640x640 пикселей")
        print("📍 Формат: PNG")
        print("=" * 50)
        print("ℹ️ Аватар будет автоматически установлен при запуске бота")
    else:
        print("❌ Не удалось создать аватар")
        print("ℹ️ Вы можете создать аватар вручную:")
        print("   1. Создайте изображение 640x640 пикселей")
        print("   2. Сохраните как 'bot_avatar.png'")
        print("   3. Положите в папку с ботом")